---
import DesktopnNavigation from "@/components/Header/DesktopNavigation.astro";
import SideBarMobile from "@/components/Header/SideBarMobile.astro";
import XIcon from "@/components/icons/Xicon.astro";
---

<header
  class="sticky rounded-b-xl top-0 backdrop-blur-lg border-b border-white/10 z-50"
>
  <nav class="container mx-auto px-4 py-4 flex justify-between items-center">
    <a
      href="#"
      class="text-xl font-bold bg-gradient-to-r from-indigo-400 to-blue-500 bg-clip-text text-transparent"
    >
      T.O.
    </a>

        <button
            id="menuButton"
            popoverTarget="mobileMenu"
            class="md:hidden cursor-pointer p-2 rounded-lg bg-white/5 border border-white/10 hover:bg-white/10"
        >
            <XIcon />
        </button>
        <!-- Desktop Navigation -->
        <DesktopnNavigation />
        <!-- Mobile Sidebar -->
        <SideBarMobile />
    </nav>
</header>
